baseurl = "https://loboto51.github.io/lobotourisme/" 
languageCode = "fr-FR"
title = "Lobotourisme"
author = "Loboto"
theme = "hugo-future-imperfect"
preserveTaxonomyNames = true
paginate = 3
disqusShortname = "shortname"
googleAnalytics = ""
pluralizeListTitles = false
# Sets the default language for multilanguage use.
defaultContentLanguage = "fr"

[params]
  # Sets the meta tag description
  description          = "Lobotourisme - Mes notes sur diverses choses que je souhaite conserver ou partager, sur les protections moto, des petites docs linux, etc. Site statique Hugo"
  # Sets the navbarTitle that appears in the top left of the navigation bar
  navbarTitle          = "Lobotourisme"
  # Sets where "View More Posts" links
  viewMorePostLink     = "/blog/"

# Optional Params
  # Sets navbarTitle to match the section of the website
  dynamicTitles        = true
  # Sets RSS icons to appear on the sidebar with the social media buttons
  rssAppearAtTop       = true
  rssAppearAtBottom    = true
  # Sets Social Media icons to appear on the sidebar
  socialAppearAtTop    = true
  socialAppearAtBottom = true
  # Sets Categories to sort by number of posts instead of alphabetical
  categoriesByCount    = true
  # set to show or to hide categories in the sidebar
  showSidebarCategories = true
  # Sets Estimated Reading Time to appear in post headers
  includeReadingTime   = true
  # Sets the Favicon and Version for the site. Default support is for
  # apple-touch-icon-precomposed.png, favicon.png, favicon.ico, and mstile.png.
  # These are stored in the favicon folder. See the following for more information:
  # https://github.com/audreyr/favicon-cheat-sheet
  loadFavicon          = true
  faviconVersion       = ""
  # Sets Social Share links to appear on posts
  socialShare          = false
  # Sets specific share to appear on posts (default behavior is to appear)
  socialShareTwitter   = false
  socialShareFacebook  = false
  socialShareReddit    = false
  socialSharePinterest = false
  socialShareEmail     = true

  socialShareGoogleplus = false
  socialShareLinkedin  = false
  socialShareStumbleupon = false

  # Load custom CSS or JavaScript files. The variable is an array so that you
  # can load multiple files if necessary. You can also load the standard theme
  # files by adding the value, "default".
  # customCSS            = ["default", "/path/to/file"]
  # customJS             = ["default", "/path/to/file"]
  customCSS            = ["default"]
  customJS             = ["default"]

  # options for highlight.js (version, additional languages, and theme)
  disable_highlight = false # set to true to disable Highlight
  highlightjsVersion = "9.11.0"
  highlightjsCDN = "//cdn.bootcss.com"
  highlightjsLang = ["r", "yaml", "css"]
  highlightjsTheme = "github"

  MathJaxCDN = "//cdn.bootcss.com"
  MathJaxVersion = "2.7.1"

   # Set to true to disable code folding
   disable_codefolding = false
   # Set to "hide" or "show" all codes by default
   codefolding_show = "hide"
   # Code blocks to apply code folding
   codeblocks = ["pre.sourceCode", "pre.r", "pre.python", "pre.bash", "pre.sql", "pre.cpp", "pre.stan", "pre.js", "pre.css", "pre.html", "pre.yaml"]

  # How to cite blog article
  disable_citation = true
  citation_author = "Loboto"

# Disqus will take priority over Staticman (github.com/eduardoboucas/staticman)
# due to its ease of use. Feel free to check out both and decide what you would
# prefer to use. See Staticman.yml for additional settings.
[params.staticman]
  # Sets Statiman to be active
  staticman = false
  # Sets the location for Staticman to connect to
  username  = ""
  repo      = ""
  branch    = ""

  [params.staticman.recaptcha]
    sitekey = "SITE_KEY"
    secret  = "ENCRYPTED_SECRET"

  # These are optional params related to the sidebar. They are recommended, but not
  # required for proper functionality. HTML is supported within the params.
[params.intro]
  header    = "Lobotourisme"

  paragraph = "Bloc-note de l'espaaace"

  about     = "Ce site me sert de bloc-note pour diverses choses que je souhaite conserver ou partager. Essentiellement mes recherches sur le net sur les protections moto, et des petites docs linux."

  # This appears at the top of the sidebar above params.intro.header.
  # A width of less than 100px is recommended from a design perspective.
  [params.intro.pic]
    src       = "/favicons/favicon-128x128.png"
    # Sets Image to be a cirlce
    circle    = false
    # Sets Image to be Future Imperfect's hexagon
    imperfect = false
    width     = "128"
    height     = "128"
    alt       = "Lobotourisme"

[params.postAmount]
# Sets the number of recent posts to show in the sidebar. The default value is 5.
    sidebar = 5


[[menu.main]]
	name = "Accueil"
	url = ""
	identifier = "fa fa-home"
	weight = 1

[[menu.main]]
	name = "Moto"
	url = "categories/moto/"
	identifier = "fa fa-moto"
	weight = 5

[[menu.main]]
	name = "Dev"
	url = "categories/dev/"
	identifier = "fa fa-dev"
	weight = 6

[[menu.main]]
	name = "Ecologie"
	url = "categories/écologie/"
	identifier = "fa fa-ecologie"
	weight = 7
	
[[menu.main]]
	name = "Contact"
	url = "contact/"
	identifier = "fa fa-envelope-o"
	weight = 8


# Sets Social Media icons to appear and link to your account. Value should be your
# username unless otherwise noted. These are the icons affected by socialAppearAtTop
# and socialAppearAtBottom.
[social]
  # Coding Communities
  github           = "loboto51"
  # Email
  email            = "example"
  

  
